doctype html
//vars == Holder.holes: int, Holder.fields: form object
html(lang="en")
 head 
  include includes/head
  title Carousel of Forms
 body
  //Carousel
  nav
   a(href="http://ecu.edu",class= "h2") things
  form(method="get")
   div(id= "myCarousel",class= "carousel slide", data-ride="carousel",data-interval=0)
    //Indicators
    ol.carousel-indicators
     each hole, i in Holder.holes
      if i == 0
       li(data-target="#myCarousel", data-slide-to=i,class="active")
      else
       li(data-target="#myCarousel", data-slide-to=i)
    div.carousel-inner 
     
     each hole, i in Holder.holes      
       
      if i == 0
       div.item.active 
        img(data-src="holder.js/480x800/auto/#777:#7a7a7a/text:"+ i,alt="First Slide")
        div.container
         each field, j in Holder.fields
          div.carousel-caption          
           if(field.required)  
            div.field.required
             label(for="id_"+i+"_"+j)
              h1=field.name
             //input(type=field.widget.type,name=field.name,id="id_" + i+"_"+j)
           else
            div.field
             label(for="id_"+i+"_"+j)
              h1=field.name
             //input(type=field.widget.type,name=field.name,id="id_" + i+"_"+j)
     
      else
       div.item
        img(data-src="holder.js/480x800/auto/#777:#7a7a7a/text:" +i, alt=i)
        div.container
         each field, j in Holder.fields
          div.carousel-caption
           if(field.required)
            div.field.required
             label(for="id_"+i + "_" + j)
              h1=field.name
             //input(type=field.widget.type,name=field.name,id="id_" + i+ "_" + j)
           else
            div.field
             label(for="id_"+i + "_" +j)
              h1=field.name
             //input(type=field.widget.type,name=field.name,id="id_" + i+ "_" + j)  
   //Controls
   a(class="left carousel-control", href="#myCarousel",data-slide="prev")
    span.glyphicon.glyphicon-chevron-left
   a(class="right carousel-control",href="#myCarousel",data-slide="next")
    span.glyphicon.glyphicon-chevron-right
  include includes/footer
 //
  table.table
   tr 
    th Player
    th Course
    th Tournament
    th Practice?
    th Hole
    th Score
    th Fairway?
    th GoPosition
    th WedgeReg
    th WedgeDist
    th WedgeRough
    th GreenInOut
    th GreenLetter
    th Putts
    th UpDownSuccess
    th UpDownBunker
    th UpDownInOut
   each row, i in Result.rows
    tr
     td=row.player
     td=row.course
     td=row.tournament
     td=row.practice
     td=row.hole
     td=row.score
     td=row.fairway
     td=row.goposition
     td=row.wedgereg
     td=row.wedgedist
     td=row.wedgerough
     td=row.greeninout
     td=row.greenletter
     td=row.putts
     td=row.updownsuccess
     td=row.updownbunker
     td=row.updowninout
    
 